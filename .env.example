# === COMMON ===
NODE_ENV=production

# === BACKEND (Express/Socket.io) ===
PORT=3001
MONGODB_URI=
JWT_SECRET=
JWT_REFRESH_SECRET=
CLIENT_URL=http://localhost:3000

# TURN / WebRTC backend config (no STUN needed — direct on LAN, TURN for cross-network)
TURN_URL=
TURN_SECRET=

# === FRONTEND (Next.js) ===
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:3001